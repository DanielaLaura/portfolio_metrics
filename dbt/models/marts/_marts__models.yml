version: 2

models:
  - name: mart_metric_pivot
    description: >
      Human-review table. Grain: one row per (canonical_company, period);
      one column per canonical metric plus quality counters (unverified,
      needs-review, restatements, cross-source conflicts).
